import streamlit as st
import pandas as pd

# --- CSS Personalizado para Layout e Estilo ---
st.markdown("""
<style>
    .block-container {
        padding-top: 2rem;
        padding-bottom: 0rem;
        padding-left: 1rem;
        padding-right: 1rem;
    }
    /* Centralizar título */
    h1 {
        text-align: center;
        color: #2C005A;
        font-family: 'Arial', sans-serif;
    }
    /* Centralizar logo */
    .logo-container {
        display: flex;
        justify-content: center;
        margin-bottom: 20px;
    }
    /* Estilo sidebar */
    section[data-testid="stSidebar"] {
        background-color: #F0F8FF;
        color: #2C005A;
    }
    /* Botões */
    button[kind="secondary"], button[kind="primary"] {
        border-radius: 8px !important;
        box-shadow: 2px 2px 5px rgba(0,0,0,0.1);
    }
    /* File uploader */
    .stFileUploader label {
        font-size: 1.1em;
        font-weight: bold;
        color: #2C005A;
    }
</style>
""", unsafe_allow_html=True)

# --- Logo centralizada ---
st.markdown('<div class="logo-container">', unsafe_allow_html=True)
st.image("01 - Dati Logotipo Oficial.png", width=200)
st.markdown('</div>', unsafe_allow_html=True)

# --- Título ---
st.title("Resumo Financeiro da AWS")

# --- Upload de arquivo ---
uploaded_file = st.file_uploader("Envie seu arquivo CSV de calculadora AWS aqui", type=["csv"])

if uploaded_file is not None:
    df = pd.read_csv(uploaded_file)
    st.success("Arquivo carregado com sucesso!")

    st.subheader("Visão Geral do Resumo")
    st.write(df.head())

    with st.container(border=True):
        st.write("Aqui viriam os gráficos ou tabelas de resumo financeiro.")
        st.metric(label="Custo Total Estimado", value="R$ 1.234,56", delta="R$ 50,00 (Semana Passada)")

    with st.sidebar:
        st.header("Opções")
        selected_option = st.radio("Escolha o período:", ("Mensal", "Semanal", "Anual"))
        st.button("Gerar Relatório Detalhado")
